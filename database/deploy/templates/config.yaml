{{- $fullName := include "app.fullname" . -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $fullName }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  initdb.sql: {{ required "Missing initDbSql value" .Values.initDbSql }}